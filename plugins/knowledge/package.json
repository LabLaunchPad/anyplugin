{
  "name": "@lablaunchpad/plugin-knowledge",
  "version": "0.1.1",
  "description": "First-party AnyPlugin plugin: OKF v0.2 knowledge layer (reader skill, capture hooks, MCP server, validator)",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b"
  },
  "dependencies": {
    "@lablaunchpad/core": "workspace:*"
  }
}

{
  "name": "@lablaunchpad/adapter-codex",
  "version": "0.1.1",
  "description": "AnyPlugin adapter: emit/install Codex CLI plugin bundles and config.toml merges",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b"
  },
  "dependencies": {
    "@lablaunchpad/core": "workspace:*",
    "smol-toml": "^1.3.0"
  }
}

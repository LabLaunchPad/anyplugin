{
  "name": "@agent-prism/adapter-codex",
  "version": "0.1.0",
  "description": "Agent-Prism adapter: emit/install Codex CLI plugin bundles and config.toml merges",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b"
  },
  "dependencies": {
    "@agent-prism/core": "workspace:*",
    "smol-toml": "^1.3.0"
  }
}
